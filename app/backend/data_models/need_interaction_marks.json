{
    "title": "need_interaction_marks",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Primary key, auto-increment"
        },
        "user_id": {
            "type": "string",
            "description": "Owner user ID"
        },
        "interaction_id": {
            "type": "integer",
            "description": "FK to interactions"
        },
        "person_need_id": {
            "type": "integer",
            "description": "FK to person_needs"
        },
        "fulfillment": {
            "type": "string",
            "description": "fulfilled, partial, missed, hurtful"
        },
        "score": {
            "type": "integer",
            "description": "Calculated score: fulfilled=+3, partial=+1, missed=0, hurtful=-2"
        },
        "created_at": {
            "type": "string",
            "format": "date-time",
            "description": "Created timestamp"
        }
    },
    "required": [
        "id",
        "user_id",
        "interaction_id",
        "person_need_id",
        "fulfillment"
    ]
}
